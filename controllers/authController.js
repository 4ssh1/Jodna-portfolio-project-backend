const User = require('../models/userModel')

const register = (req, res, next)=>{
    try {
        const {firstname, lastname, email, password, profilePic, bio, views} = req.body
        
        
    } catch (error) {
        
    }
}